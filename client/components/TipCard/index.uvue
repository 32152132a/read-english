<template>
  <view class="tip-card">
    <text class="tip-card__icon">!</text>
    <text class="tip-card__content">
      <text class="tip-card__title">{{ title }}</text>
      <text>　{{ text }}</text>
    </text>
  </view>
</template>

<script lang="uts">
export default {
  props: {
    title: { type: String, default: '提示' },
    text: { type: String, required: true },
  },
}
</script>

<style lang="scss">
@import '../../uni.scss';

.tip-card {
  margin-top: 20rpx;
  padding: 16rpx 20rpx;
  border-radius: 14rpx;
  background-color: #eef2ff;
  display: grid;
  grid-template-columns: 28rpx 1fr;
  align-items: start;
  gap: 12rpx;

  &__icon {
    width: 28rpx;
    height: 28rpx;
    margin-top: 2rpx;
    border-radius: 50%;
    background-color: $color-primary;
    color: $color-surface;
    text-align: center;
    font-size: 20rpx;
    font-weight: 600;
    line-height: 28rpx;
  }

  &__content {
    color: $color-text-secondary;
    font-size: 22rpx;
    line-height: 32rpx;
  }

  &__title {
    color: $color-primary;
    font-weight: 600;
  }
}
</style>
