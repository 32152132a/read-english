<template>
  <view class="correction-practice">
    <view class="correction-practice__word">
      <view>
        <text class="correction-practice__word-text">{{ data.word }}</text>
        <PhonemeDisplay :segments="data.ipaSegments" size="mini" />
      </view>
      <PlayButton :src="data.audioUrl" text="标准发音" size="mini" icon="volume" />
    </view>
    <RecordingPanel
      title="重新跟读"
      description="点击录音，读出上方单词（最长 4 秒）"
      active-description="再次点击可提前结束"
      @start="$emit('start')"
      @finish="$emit('finish')"
    />
  </view>
</template>

<script setup lang="uts">
import PlayButton from '../PlayButton/index.uvue'
import PhonemeDisplay from '../PhonemeDisplay/index.uvue'
import RecordingPanel from '../RecordingPanel/index.uvue'
import type { CorrectionPracticeProps } from '../../src/config/types.uts'

defineProps<CorrectionPracticeProps>()
defineEmits(['start', 'finish'])
</script>

<style lang="scss">
@import '../../uni.scss';

.correction-practice {
  display: grid;
  grid-template-columns: minmax(0, 1fr);
  gap: 10rpx;
  padding: 28rpx;
  border: 1rpx solid $color-border;
  border-radius: 18rpx;
  background: #fff;

  &__word {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    align-items: center;
    gap: 16rpx;
  }

  &__word-text {
    display: block;
    margin-bottom: 8rpx;
    font-size: 36rpx;
    font-weight: 700;
  }
}
</style>
