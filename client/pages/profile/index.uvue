<template>
  <view class="screen">
    <view class="page profile">
      <text class="title">{{ pageData.title }}</text>
      <view class="card user">
        <view class="avatar">{{ pageData.user.avatarText }}</view>
        <view>
          <text class="name">{{ pageData.user.nickname }}</text>
          <text class="muted">{{ pageData.user.learningStatus }}</text>
        </view>
        <text class="arrow">›</text>
      </view>
      <view class="card stats">
        <view v-for="stat in pageData.stats" :key="stat.code">
          <text>{{ stat.label }}</text>
          <b>{{ stat.value }}</b>
          <text>{{ stat.suffix }}</text>
        </view>
      </view>
      <text class="section-title">学习记录</text>
      <view class="card menu">
        <view v-for="item in pageData.records" :key="item.code">
          {{ item.label }}
          <text>›</text>
        </view>
      </view>
      <text class="section-title">设置</text>
      <view class="card menu">
        <view v-for="item in pageData.settings" :key="item.code">
          {{ item.label }}
          <text>›</text>
        </view>
      </view>
      <text class="version">{{ pageData.version }}</text>
    </view>
    <BottomNav active="profile" />
  </view>
</template>
<script lang="uts">
import BottomNav from '../../components/BottomNav/index.uvue'
export default {
  components: { BottomNav },
  data() {
    return {
      pageData: {
        title: '我的',
        user: { avatarText: 'YQ', nickname: '永庆', learningStatus: '正在完成基础学习' },
        stats: [
          { code: 'learned', label: '已学习', value: 8, suffix: '/ 20' },
          { code: 'streak', label: '连续学习', value: 3, suffix: '天' },
          { code: 'review', label: '待复习', value: 2, suffix: '个' },
        ],
        records: [
          { code: 'progress', label: '▧　学习进度' },
          { code: 'mastered', label: '/iː/　已掌握音标' },
          { code: 'favorites', label: '☆　收藏单词' },
        ],
        settings: [
          { code: 'accent', label: '🔊　发音偏好　　　　　　　 美式英语' },
          { code: 'audio-cache', label: '⇩　音频缓存' },
          { code: 'feedback', label: '□　意见反馈' },
          { code: 'about', label: 'ⓘ　关于产品' },
        ],
        version: 'v1.0.0',
      },
    }
  },
}
</script>
<style lang="scss">
@import '../../uni.scss';
.profile {
  padding-top: 110rpx;
}
.title {
  font-size: 54rpx;
  font-weight: 700;
}
.user {
  margin-top: 30rpx;
  display: flex;
  align-items: center;
  gap: 26rpx;
}
.avatar {
  width: 108rpx;
  height: 108rpx;
  border-radius: 50%;
  background: #e7ebff;
  color: $color-primary;
  font-size: 38rpx;
  font-weight: 700;
  text-align: center;
  line-height: 108rpx;
}
.name {
  display: block;
  font-size: 32rpx;
  font-weight: 700;
}
.arrow {
  margin-left: auto;
  font-size: 48rpx;
  color: $color-text-secondary;
}
.stats {
  margin-top: 26rpx;
  display: flex;
  justify-content: space-around;
}
.stats view {
  display: flex;
  flex-direction: column;
  align-items: center;
  color: $color-text-secondary;
}
.stats b {
  margin: 12rpx;
  font-size: 42rpx;
  color: $color-text-primary;
}
.menu {
  padding: 0 28rpx;
}
.menu view {
  padding: 26rpx 6rpx;
  border-bottom: 1rpx solid $color-border;
  display: flex;
  justify-content: space-between;
}
.menu view:last-child {
  border: 0;
}
.version {
  display: block;
  margin: 34rpx;
  text-align: center;
  color: #a2a9bc;
}
</style>
