<template>
  <view class="word-list-cards">
    <view v-for="item in items" :key="item.id" class="word-list-cards__card">
      <view class="word-list-cards__icon" :class="'is-' + item.tone">{{ item.icon }}</view>
      <view class="word-list-cards__content">
        <text class="word-list-cards__title">{{ item.title }}</text>
        <text class="word-list-cards__description">{{ item.description }}</text>
        <ProgressBar :current="item.completed" :total="item.total" />
      </view>
      <button class="outline-button word-list-cards__button" @click="$emit('select', item.id)">
        {{ item.completed > 0 ? '继续学习' : '开始学习' }}
      </button>
    </view>
  </view>
</template>

<script lang="uts">
import ProgressBar from '../ProgressBar/index.uvue'

export default {
  components: { ProgressBar },
  props: { items: { type: Array, default: () => [] } },
  emits: ['select'],
}
</script>

<style lang="scss">
@import '../../uni.scss';

.word-list-cards {
  display: grid;
  gap: 20rpx;

  &__card {
    display: grid;
    grid-template-columns: 100rpx 1fr auto;
    align-items: center;
    gap: 22rpx;
    padding: 24rpx;
    border: 1rpx solid $color-border;
    border-radius: 18rpx;
    background: #fff;
  }

  &__icon {
    width: 100rpx;
    height: 100rpx;
    border-radius: 16rpx;
    background: #eef2ff;
    color: $color-primary;
    font-size: 28rpx;
    line-height: 100rpx;
    text-align: center;

    &.is-green {
      background: #ecfaf3;
      color: $color-success;
    }

    &.is-purple {
      background: #f5f0ff;
      color: #7a55e8;
    }
  }

  &__content {
    min-width: 0;
    display: grid;
    gap: 8rpx;
  }

  &__title {
    font-size: 30rpx;
    font-weight: 700;
  }

  &__description {
    color: $color-text-secondary;
  }

  &__button {
    min-height: 70rpx;
    margin: 0;
    padding: 0 18rpx;
    font-size: 24rpx;
  }
}
</style>
