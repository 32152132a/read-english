# 项目开发约定

## 前端组件

- 页面优先负责组件组合和数据传入，视觉样式与内部交互由组件维护，页面内尽量不写 CSS。
- 通用交互组件对外保持极简；弹窗等命令式组件通过 `ref` 暴露单一入口方法，由组件内部维护显示状态、初始化数据以及成功/失败回调。
- 只在出现真实复用需求时抽取组件，不创建仅用于转发参数的无意义中间层。

## 样式

- 布局优先使用 CSS Grid；不适合 Grid 时使用 Flex，再不济才考虑 `transform`。
- 避免使用 `position` 和 `float` 完成常规布局；仅在浮层、遮罩等确有层叠语义的场景使用定位。
- 样式使用 SCSS，并按组件根类嵌套组织；类名表达组件、元素和状态，避免污染全局样式。
- 优先依赖继承和全局设计变量，避免为每个 `view`、`text` 重复声明字体、字号和颜色。
- 页面和组件样式保持短小，只覆盖相对基础样式的差异，不堆叠重复规则。
