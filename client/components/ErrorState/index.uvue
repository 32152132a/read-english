<template>
  <view class="error-state">
    <text class="error-state__code">{{ code }}</text>
    <text class="error-state__title">{{ title }}</text>
    <text class="error-state__description">{{ description }}</text>
    <view class="error-state__actions">
      <button class="primary-button error-state__button" @click="$emit('primary')">
        {{ primaryText }}
      </button>
      <button class="outline-button error-state__button" @click="$emit('secondary')">
        {{ secondaryText }}
      </button>
    </view>
  </view>
</template>

<script lang="uts">
export default {
  props: {
    code: { type: String, required: true },
    title: { type: String, required: true },
    description: { type: String, required: true },
    primaryText: { type: String, required: true },
    secondaryText: { type: String, default: '返回首页' },
  },
  emits: ['primary', 'secondary'],
}
</script>

<style lang="scss">
@import '../../uni.scss';

.error-state {
  min-height: 100vh;
  padding: 0 44rpx;
  box-sizing: border-box;
  display: grid;
  grid-template-rows: 1fr auto auto auto 1fr;
  justify-items: center;
  text-align: center;

  &__code {
    align-self: end;
    color: $color-primary;
    font-size: 160rpx;
    font-weight: 700;
    line-height: 1;
  }

  &__title {
    margin-top: 36rpx;
    font-size: 36rpx;
    font-weight: 600;
  }

  &__description {
    margin-top: 16rpx;
    color: $color-text-secondary;
    font-size: 26rpx;
    line-height: 1.6;
  }

  &__actions {
    align-self: start;
    width: 100%;
    margin-top: 52rpx;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 24rpx;
  }

  &__button {
    width: 100%;
    margin: 0;
  }
}
</style>
