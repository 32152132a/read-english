<template>
  <view class="next-stage-list card">
    <text v-if="lockedText" class="next-stage-list__locked">{{ lockedText }}</text>
    <view class="next-stage-list__stages">
      <template v-for="(stage, index) in stages" :key="stage.code">
        <text v-if="index > 0" class="next-stage-list__arrow">›</text>
        <view class="next-stage-list__item" @click="$emit('select', stage.route)">
          <AppIcon :name="stage.iconName" :size="58" tone="dark" />
          <text class="next-stage-list__name">{{ stage.name }}</text>
          <text class="next-stage-list__description">{{ stage.description }}</text>
        </view>
      </template>
    </view>
  </view>
</template>

<script lang="uts">
export default {
  props: {
    stages: { type: Array, required: true },
    lockedText: { type: String, default: '' },
  },
  emits: ['select'],
}
</script>

<style lang="scss">
@import '../../uni.scss';

.next-stage-list {
  width: 100%;
  min-height: 238rpx;
  padding: 14rpx 30rpx 28rpx;
  box-sizing: border-box;
  display: grid;
  grid-template-columns: minmax(0, 1fr);
  grid-template-rows: auto 1fr;

  &__stages {
    min-width: 0;
    justify-self: stretch;
    align-self: center;
    display: grid;
    grid-template-columns: 1fr 30rpx 1fr 30rpx 1fr;
    align-items: center;
    justify-items: center;
  }

  &__item {
    display: grid;
    grid-template-columns: minmax(0, 1fr);
    justify-items: center;
    gap: 10rpx;
  }

  &__name {
    font-size: 27rpx;
    font-weight: 600;
  }

  &__description {
    color: $color-text-secondary;
    font-size: 21rpx;
  }

  &__arrow {
    color: $color-text-secondary;
    font-size: 42rpx;
    transform: translateY(-4rpx);
  }

  &__locked {
    justify-self: end;
    padding: 8rpx 14rpx;
    border-radius: 8rpx;
    background: #f1f3f8;
    color: $color-text-secondary;
    font-size: 20rpx;
  }
}
</style>
