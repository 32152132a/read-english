<template>
  <view class="recent-learning">
    <view v-for="item in items" :key="item.id" class="recent-learning__item">
      <text>{{ item.word }}</text>
      <text class="recent-learning__time">◷ {{ item.learnedAt }}</text>
    </view>
  </view>
</template>

<script lang="uts">
export default { props: { items: { type: Array, default: () => [] } } }
</script>

<style lang="scss">
@import '../../uni.scss';

.recent-learning {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 16rpx;

  &__item {
    display: grid;
    gap: 8rpx;
    padding: 22rpx;
    border: 1rpx solid $color-border;
    border-radius: 14rpx;
    background: #fff;
  }

  &__time {
    color: $color-text-secondary;
    font-size: 22rpx;
  }
}
</style>
