<template>
  <view class="page home-page">
    <view class="safe-area"></view>
    <text class="eyebrow">每天进步一点点</text>
    <text class="title">你好，开始今天的发音练习</text>

    <view class="card hero-card">
      <text class="hero-label">今日任务</text>
      <text class="hero-title">认识第一个音标</text>
      <text class="hero-description">从口型、听音和例词开始学习</text>
      <button class="primary-button" @click="startLearning">开始学习</button>
    </view>

    <text class="section-title">学习路径</text>
    <view class="card path-card">
      <text class="path-title">基础发音</text>
      <text class="path-description">音标总览 · 音素与音节 · 拼读训练</text>
    </view>
  </view>
</template>

<script lang="uts">
export default {
  methods: {
    startLearning() {
      uni.navigateTo({ url: '/pages/phoneme/detail' })
    },
  },
}
</script>

<style lang="scss">
@import '../../uni.scss';

.safe-area { height: 88rpx; }
.eyebrow { color: $color-primary; font-size: 28rpx; }
.title { display: block; margin-top: 12rpx; font-size: 48rpx; font-weight: 700; line-height: 1.3; }
.hero-card { margin-top: 48rpx; background-color: $color-primary; border-color: $color-primary; color: #fff; }
.hero-label { font-size: 24rpx; opacity: 0.8; }
.hero-title { display: block; margin-top: 16rpx; font-size: 40rpx; font-weight: 700; }
.hero-description { display: block; margin-top: 12rpx; font-size: 28rpx; opacity: 0.84; }
.primary-button { margin-top: 40rpx; min-height: 88rpx; border-radius: 28rpx; background-color: #fff; color: $color-primary; font-size: 28rpx; }
.section-title { display: block; margin: 48rpx 0 20rpx; font-size: 34rpx; font-weight: 600; }
.path-title { display: block; font-size: 32rpx; font-weight: 600; }
.path-description { display: block; margin-top: 12rpx; color: $color-text-secondary; font-size: 26rpx; }
</style>
