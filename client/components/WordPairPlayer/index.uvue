<template>
  <view class="word-pair-player card">
    <template v-for="(item, index) in items" :key="item.word">
      <view class="word-pair-player__item" :class="index === 1 ? 'is-reverse' : ''">
        <PlayButton v-if="index === 0" :src="item.audioUrl" size="mini" />
        <view class="word-pair-player__content">
          <text class="word-pair-player__word">{{ item.word }}</text>
          <PhonemeDisplay :segments="item.segments" size="mini" />
        </view>
        <PlayButton v-if="index === 1" :src="item.audioUrl" size="mini" color="#11aaa0" />
      </view>
      <text v-if="index === 0" class="word-pair-player__divider">⇄</text>
    </template>
  </view>
</template>

<script lang="uts">
import PhonemeDisplay from '../PhonemeDisplay/index.uvue'
import PlayButton from '../PlayButton/index.uvue'

export default {
  components: { PhonemeDisplay, PlayButton },
  props: {
    items: { type: Array, required: true },
  },
}
</script>

<style lang="scss">
@import '../../uni.scss';

.word-pair-player {
  padding: 22rpx 28rpx;
  display: grid;
  grid-template-columns: 1fr 52rpx 1fr;
  align-items: center;
  gap: 12rpx;

  &__item {
    display: grid;
    grid-template-columns: auto 1fr;
    align-items: center;
    justify-items: center;
    gap: 14rpx;

    &.is-reverse {
      grid-template-columns: 1fr auto;
    }
  }

  &__content {
    display: grid;
    grid-template-columns: minmax(0, 1fr);
    justify-items: start;
    gap: 4rpx;
  }

  &__word {
    font-size: 30rpx;
    font-weight: 700;
  }

  &__divider {
    width: 52rpx;
    height: 52rpx;
    border-radius: 50%;
    background-color: #f0f2f7;
    color: $color-text-secondary;
    text-align: center;
    font-size: 34rpx;
    line-height: 52rpx;
  }
}
</style>
