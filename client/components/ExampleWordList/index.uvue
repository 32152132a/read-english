<template>
  <view class="example-word-list card">
    <view v-for="item in items" :key="item.word" class="example-word-list__item">
      <PlayButton :src="item.audioUrl" size="tiny" />
      <text class="example-word-list__word">{{ item.word }}</text>
      <PhonemeDisplay
        class="example-word-list__phoneme"
        :segments="item.segments"
        size="mini"
      />
      <text class="example-word-list__meaning">{{ item.zh }}</text>
    </view>
  </view>
</template>

<script lang="uts">
import PhonemeDisplay from '../PhonemeDisplay/index.uvue'
import PlayButton from '../PlayButton/index.uvue'

export default {
  components: { PhonemeDisplay, PlayButton },
  props: {
    items: { type: Array, required: true },
  },
}
</script>

<style lang="scss">
@import '../../uni.scss';

.example-word-list {
  padding: 0;

  &__item {
    min-height: 78rpx;
    padding: 0 26rpx;
    border-bottom: 1rpx solid $color-border;
    display: grid;
    grid-template-columns: 40rpx minmax(120rpx, 1fr) minmax(0, 1fr) 90rpx;
    align-items: center;
    gap: 16rpx;

    &:last-child {
      border-bottom: 0;
    }
  }

  &__word {
    font-weight: 600;
  }

  &__phoneme {
    justify-self: center;
  }

  &__meaning {
    color: $color-text-secondary;
    text-align: right;
  }
}
</style>
