<template>
  <view class="pronunciation-mappings">
    <view v-for="item in items" :key="item.part" class="pronunciation-mappings__item">
      <text class="pronunciation-mappings__part">{{ item.part }}</text>
      <text>→</text>
      <PhonemeDisplay :segments="item.segments" size="mini" />
      <text class="pronunciation-mappings__tip">{{ item.tip }}</text>
    </view>
  </view>
</template>

<script lang="uts">
import PhonemeDisplay from '../PhonemeDisplay/index.uvue'

export default {
  components: { PhonemeDisplay },
  props: { items: { type: Array, default: () => [] } },
}
</script>

<style lang="scss">
@import '../../uni.scss';

.pronunciation-mappings {
  border: 1rpx solid $color-border;
  border-radius: 18rpx;
  background: #fff;
  overflow: hidden;

  &__item {
    display: grid;
    grid-template-columns: 90rpx 30rpx 120rpx 1fr;
    align-items: center;
    gap: 12rpx;
    padding: 24rpx;
    border-bottom: 1rpx solid $color-border;

    &:last-child {
      border-bottom: 0;
    }
  }

  &__part {
    padding: 10rpx;
    border-radius: 8rpx;
    background: #f1f4ff;
    color: $color-primary;
    text-align: center;
  }

  &__tip {
    color: $color-text-secondary;
  }
}
</style>
