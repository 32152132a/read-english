<template>
  <view class="quick-access-list card">
    <view
      v-for="item in items"
      :key="item.code"
      class="quick-access-list__item"
      @click="$emit('select', item.route)"
    >
      <AppIcon :name="item.iconName" :size="52" tone="dark" />
      <view class="quick-access-list__content">
        <text class="quick-access-list__name">{{ item.name }}</text>
        <text class="quick-access-list__description">{{ item.description }}</text>
      </view>
    </view>
  </view>
</template>

<script setup lang="uts">
import type { QuickAccessListProps } from '../../src/config/types.uts'

defineProps<QuickAccessListProps>()
defineEmits(['select'])
</script>

<style lang="scss">
@import '../../uni.scss';

.quick-access-list {
  width: 100%;
  padding: 24rpx;
  box-sizing: border-box;
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 18rpx;

  &__item {
    min-width: 0;
    display: grid;
    grid-template-columns: minmax(0, 1fr);
    justify-items: center;
    gap: 12rpx;
  }

  &__content {
    min-width: 0;
    display: grid;
    grid-template-columns: minmax(0, 1fr);
    justify-items: center;
    gap: 6rpx;
  }

  &__name {
    text-align: center;
    font-size: 25rpx;
    font-weight: 600;
  }

  &__description {
    color: $color-text-secondary;
    text-align: center;
    font-size: 20rpx;
  }
}
</style>
