<template>
  <view class="tip-card" :class="variant === 'memory' ? 'tip-card--memory' : ''">
    <AppIcon name="lightbulb" :size="34" />
    <view class="tip-card__content">
      <text class="tip-card__title">{{ title }}</text>
      <text class="tip-card__text">{{ text }}</text>
    </view>
  </view>
</template>

<script lang="uts">
export default {
  props: {
    title: { type: String, default: '提示' },
    text: { type: String, required: true },
    variant: { type: String, default: 'default' },
  },
}
</script>

<style lang="scss">
@import '../../uni.scss';

.tip-card {
  margin-top: 20rpx;
  padding: 16rpx 20rpx;
  border-radius: 14rpx;
  background-color: #eef2ff;
  display: grid;
  grid-template-columns: 34rpx 1fr;
  align-items: start;
  gap: 12rpx;

  &__content {
    display: grid;
    gap: 8rpx;
  }

  &__title {
    color: $color-primary;
    font-weight: 600;
  }

  &__text {
    font-size: 23rpx;
    line-height: 34rpx;
  }

  &--memory {
    margin-top: 28rpx;
    padding: 20rpx 24rpx;
    border: 2rpx solid $color-border;
    border-radius: 16rpx;
    background-color: $color-surface;
  }
}
</style>
