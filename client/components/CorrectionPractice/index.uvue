<template>
  <view class="correction-practice">
    <view class="correction-practice__word">
      <PlayButton :src="data.audioUrl" size="mini" />
      <text>{{ data.word }}</text>
      <PhonemeDisplay :segments="data.ipaSegments" size="mini" />
    </view>
    <view class="correction-practice__record" @click="$emit('record')">
      <AppIcon name="mic" :size="34" />
      <view>
        <text class="correction-practice__record-title">点击录音跟读</text>
        <text class="correction-practice__record-tip">建议跟读 2–3 次</text>
      </view>
    </view>
  </view>
</template>

<script setup lang="uts">
import PlayButton from '../PlayButton/index.uvue'
import PhonemeDisplay from '../PhonemeDisplay/index.uvue'
import type { CorrectionPracticeProps } from '../../src/config/types.uts'

defineProps<CorrectionPracticeProps>()
defineEmits(['record'])
</script>

<style lang="scss">
@import '../../uni.scss';

.correction-practice {
  display: grid;
  grid-template-columns: minmax(0, 1fr);
  gap: 22rpx;
  padding: 26rpx;
  border: 1rpx solid $color-border;
  border-radius: 18rpx;
  background: #fff;

  &__word {
    display: flex;
    flex-direction: row;
    align-items: center;
    gap: 16rpx;
    font-size: 30rpx;
  }

  &__record {
    width: 100%;
    padding: 20rpx;
    box-sizing: border-box;
    border-radius: 14rpx;
    background-color: #f1f4ff;
    display: grid;
    grid-template-columns: 34rpx minmax(0, 1fr);
    align-items: center;
    gap: 14rpx;
  }

  &__record-title,
  &__record-tip {
    display: block;
  }

  &__record-title {
    font-weight: 600;
  }

  &__record-tip {
    margin-top: 4rpx;
    color: $color-text-secondary;
    font-size: 22rpx;
  }
}
</style>
