<template>
  <view class="learning-stats">
    <view v-for="item in items" :key="item.code" class="learning-stats__item">
      <text>{{ item.label }}</text>
      <text class="learning-stats__value">{{ item.value }}</text>
      <text>{{ item.suffix }}</text>
    </view>
  </view>
</template>

<script lang="uts">
export default { props: { items: { type: Array, default: () => [] } } }
</script>

<style lang="scss">
@import '../../uni.scss';

.learning-stats {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  margin-top: 26rpx;
  padding: 28rpx;
  border: 1rpx solid $color-border;
  border-radius: 18rpx;
  background: #fff;

  &__item {
    display: grid;
    justify-items: center;
    gap: 8rpx;
    color: $color-text-secondary;
  }

  &__value {
    color: $color-text-primary;
    font-size: 42rpx;
    font-weight: 700;
  }
}
</style>
