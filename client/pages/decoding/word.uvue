<template>
  <view class="page">
    <LessonProgress :current="5" :total="12" />
    <text class="page-title centered">根据字母组合推测读音</text>
    <TipCard text="非重读音节中的 o 常弱读为 /ə/" />
    <view class="word-parts">
      <text>com</text>
      <text class="focus">ˈpo</text>
      <text>nent</text>
    </view>
    <view class="tiles">
      <text>com</text>
      <text>po</text>
      <text>nent</text>
    </view>
    <text class="muted centered">拖动分隔符调整拆分</text>
    <text class="section-title">字母与发音</text>
    <view class="card mappings">
      <view v-for="m in mappings" :key="m.part">
        <text class="part">{{ m.part }}</text>
        <text>→</text>
        <text>{{ m.ipa }}</text>
        <text class="muted">{{ m.tip }}</text>
      </view>
    </view>
    <StepActions :current="5" :total="12" @change="loadStep" />
  </view>
</template>
<script lang="uts">
import LessonProgress from '../../components/LessonProgress/index.uvue'
import TipCard from '../../components/TipCard/index.uvue'
import StepActions from '../../components/StepActions/index.uvue'
export default {
  components: { LessonProgress, TipCard, StepActions },
  data() {
    return {
      mappings: [
        { part: 'com', ipa: '/kəm/', tip: '辅音 + o 弱读 /ə/' },
        { part: 'po', ipa: '/pəʊ/', tip: '开音节 o 发 /əʊ/' },
        { part: 'nent', ipa: '/nənt/', tip: '结尾 t 不发音' },
      ],
    }
  },
  methods: {
    loadStep(target: number) {
      if (target < 5) uni.navigateBack()
    },
  },
}
</script>
<style lang="scss">
@import '../../uni.scss';
.centered {
  display: block;
  text-align: center;
  margin-top: 48rpx;
}
.word-parts {
  display: flex;
  justify-content: center;
  margin: 50rpx 0;
  font-size: 66rpx;
  color: #91a5e8;
}
.focus {
  color: $color-primary;
  font-weight: 700;
}
.tiles {
  display: flex;
  gap: 28rpx;
  justify-content: center;
}
.tiles text {
  padding: 26rpx 46rpx;
  border: 1rpx solid #cbd4ef;
  border-radius: 18rpx;
  color: $color-primary;
  font-size: 30rpx;
}
.mappings {
  padding: 0;
}
.mappings view {
  padding: 24rpx;
  border-bottom: 1rpx solid $color-border;
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.part {
  padding: 10rpx;
  background: #f1f4ff;
  color: $color-primary;
  border-radius: 8rpx;
}
</style>
