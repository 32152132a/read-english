<template>
  <view class="pronunciation-guide card">
    <text class="pronunciation-guide__title">怎么发音</text>
    <view class="pronunciation-guide__content">
      <MouthShape :type="mouthType" :size="mouthSize" :tone="mouthTone" />
      <view class="pronunciation-guide__steps">
        <view v-for="step in steps" :key="step" class="pronunciation-guide__step">
          <view class="pronunciation-guide__marker" />
          <text>{{ step }}</text>
        </view>
      </view>
    </view>
  </view>
</template>

<script setup lang="uts">
import type { PronunciationGuideProps } from '../../src/config/types.uts'

withDefaults(defineProps<PronunciationGuideProps>(), {
  mouthType: 'open',
  mouthSize: 'large',
  mouthTone: 'primary',
})
</script>

<style lang="scss">
@import '../../uni.scss';

.pronunciation-guide {
  padding: 24rpx 28rpx 28rpx;
  display: grid;
  grid-template-columns: minmax(0, 1fr);
  justify-items: center;

  &__title {
    display: block;
    justify-self: start;
    font-size: 28rpx;
    font-weight: 700;
  }

  &__content {
    width: 100%;
    display: grid;
    grid-template-columns: 300rpx minmax(0, 1fr);
    align-items: center;
    gap: 18rpx;
  }

  &__steps {
    width: 100%;
    display: grid;
    grid-template-columns: minmax(0, 1fr);
    gap: 14rpx;
    font-size: 25rpx;
  }

  &__step {
    width: 100%;
    display: grid;
    grid-template-columns: 10rpx minmax(0, 1fr);
    align-items: center;
    gap: 14rpx;
    color: $color-text-secondary;
    line-height: 36rpx;
  }

  &__marker {
    width: 8rpx;
    height: 8rpx;
    border-radius: 50%;
    background-color: $color-primary;
  }
}
</style>
