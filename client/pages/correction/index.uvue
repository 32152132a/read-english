<template>
  <view class="page">
    <text class="badge">{{ pageData.badge }}</text>
    <text class="headline">
      你的
      <text class="primary">{{ pageData.phoneme }}</text>
      需要加强
    </text>
    <text class="muted sub">{{ pageData.summary }}</text>
    <CorrectionIssue
      :issue="pageData.issue"
      :words="pageData.affectedWords"
      :segments="pageData.phonemeSegments"
    />
    <text class="section-title">纠音要点</text>
    <InfoSteps :items="pageData.tips" />
    <text class="section-title">迷你练习</text>
    <CorrectionPractice :data="pageData.practice" />
    <button class="primary-button full">开始专项练习</button>
    <button class="outline-button full">稍后复习</button>
  </view>
</template>
<script lang="uts">
import CorrectionIssue from '../../components/CorrectionIssue/index.uvue'
import InfoSteps from '../../components/InfoSteps/index.uvue'
import CorrectionPractice from '../../components/CorrectionPractice/index.uvue'

export default {
  components: { CorrectionIssue, InfoSteps, CorrectionPractice },
  data() {
    return {
      pageData: {
        badge: '✦ AI 建议',
        phoneme: '/θ/',
        phonemeSegments: [{ text: '/θ/', tone: 'primary' }],
        summary: '根据最近 6 次跟读结果生成',
        issue: '容易读成 /s/',
        affectedWords: ['think', 'authentication', 'method'],
        tips: [
          { title: '舌尖轻触上下齿之间', text: '舌尖放在上下门齿之间，轻轻接触' },
          { title: '先送气再发声', text: '先让气流从齿间通过，再发出声音' },
          { title: '不要读成「丝」', text: '避免舌尖离开或顶住上齿龈' },
        ],
        practice: {
          word: 'think',
          ipaSegments: [{ text: '/θɪŋk/', tone: 'muted' }],
          audioUrl: '',
        },
      },
    }
  },
}
</script>
<style lang="scss">
@import '../../uni.scss';

.badge {
  padding: 8rpx 14rpx;
  border: 1rpx solid #a6b6ff;
  border-radius: 10rpx;
  color: $color-primary;
}
.headline {
  display: block;
  margin-top: 28rpx;
  font-size: 42rpx;
  font-weight: 700;
}
.sub {
  display: block;
  margin: 14rpx 0 34rpx;
}

.full {
  width: 100%;
  margin-top: 18rpx;
}
</style>
