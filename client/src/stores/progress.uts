import { defineStore } from 'pinia'

export const useProgressStore = defineStore('progress', () => {
  const pendingCount = ref(0)

  function markPending() {
    pendingCount.value += 1
  }

  return { pendingCount, markPending }
})
